import { OnInit, OnDestroy } from '@angular/core';
import { Subject } from 'rxjs';
import { FilestackService } from '../filestack.service';
import { PickerInstance, PickerOptions, ClientOptions, InputFile, PickerResponse, FilestackError } from 'filestack-js';
export declare abstract class PickerBaseComponent implements OnInit, OnDestroy {
    protected filestackService: FilestackService;
    apikey: string;
    pickerOptions: PickerOptions;
    clientOptions: ClientOptions;
    file: InputFile;
    source: string;
    uploadSuccess: Subject<PickerResponse>;
    uploadError: Subject<FilestackError>;
    picker: PickerInstance;
    constructor(filestackService: FilestackService);
    ngOnInit(): void;
    ngOnDestroy(): void;
}
